name: Performance Grade Tests
on:
  push:
    branches: [main]
jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run grade tests
        run: python tests/grade_rules.py
