name: Performance Analyzer Python Check
on:
  push:
    branches: [main]
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python -m py_compile tests/grade_rules.py tests/repository_smoke.py
