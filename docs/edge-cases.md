# Edge Cases

- Empty datasets should produce a clear empty-state result.
- Missing subject values should not silently become zero.
- Tied highest or lowest scores should be reported consistently.
- Small datasets should still use the same calculation rules as larger datasets.